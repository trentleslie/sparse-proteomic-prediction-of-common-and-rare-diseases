stages:
  - sync

variables:
  GITLAB_USER_EMAIL: trent.leslie@phenomehealth.org
  GITHUB_USER_EMAIL: trentleslie@gmail.com
  GIT_STRATEGY: clone
  GIT_DEPTH: 1

sync_gitlab_to_github:
  stage: sync
  image: python:3.10  # Ensures that Python 3.10 is used
  script:
    - python --version  # Confirm the Python version
    - git config --global user.email "$GITHUB_USER_EMAIL"
    - git config --global user.name "Trent Leslie"
    - git remote add github https://github.com/trentleslie/sparse-proteomic-prediction-of-common-and-rare-diseases.git
    - git fetch gitlab || exit 1
    - git checkout main || exit 1
    - git merge gitlab/main || exit 1
    - git push github main || exit 1
    - git push github main --tags || exit 1
  only:
    - main
